// This is automatically generated by PlayFab SDKGenerator. DO NOT modify this manually!
#include "PlayFabProxyPrivatePCH.h"
#include "PlayFab.h"
#include "PFServerUpdateBans.h"

UPFServerUpdateBans::UPFServerUpdateBans(const FObjectInitializer& ObjectInitializer)
	: Super(ObjectInitializer)
	, SuccessDelegate(PlayFab::UPlayFabServerAPI::FUpdateBansDelegate::CreateUObject(this, &ThisClass::OnSuccessCallback))
	, ErrorDelegate(PlayFab::FPlayFabErrorDelegate::CreateUObject(this, &ThisClass::OnErrorCallback))
{
}

UPFServerUpdateBans* UPFServerUpdateBans::UpdateBans(UObject* WorldContextObject, class APlayerController* PlayerController , const TArray<FBPServerUpdateBanRequest>& InBans)
{
	UPFServerUpdateBans* Proxy = NewObject<UPFServerUpdateBans>();
 	//Proxy->PlayerControllerWeakPtr = PlayerController;
	for (const FBPServerUpdateBanRequest& elem : InBans)
    {
        Proxy->Request.Bans.Add(elem.Data);
    }

	

 	//Proxy->WorldContextObject = WorldContextObject;
	return Proxy;
}

void UPFServerUpdateBans::Activate()
{
	// grab the module, so we can get a valid pointer to the client API 
	PlayFabServerPtr ServerAPI = IPlayFabModuleInterface::Get().GetServerAPI();

	bool CallResult = false;

	if(ServerAPI.IsValid())
	{	
		CallResult = ServerAPI->UpdateBans(Request, SuccessDelegate, ErrorDelegate);
	}

	if(CallResult == false)
	{
		FBPServerUpdateBansResult BPResult;
 		OnFailure.Broadcast(BPResult);
	}
}


//////////////////////////////////////////////////////////////////////////
// Delegate handles
void UPFServerUpdateBans::OnSuccessCallback(const PlayFab::ServerModels::FUpdateBansResult& Result)
{
	FBPServerUpdateBansResult BPResult;
 	BPResult.Data = Result;
 	OnSuccess.Broadcast(BPResult);
}

void UPFServerUpdateBans::OnErrorCallback(const PlayFab::FPlayFabError& InError)
{
	FBPServerUpdateBansResult BPResult;
 	OnFailure.Broadcast(BPResult);	
}
